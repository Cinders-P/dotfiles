# python packages and duckdb added to path
export PATH="$HOME/.local/bin:$HOME/.duckdb/cli/latest:$PATH"

# silent failure if key doesn't exist
eval "$(keychain --eval --quiet ~/.ssh/id_ed25519 2>/dev/null || true)"

